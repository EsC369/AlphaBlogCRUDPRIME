<!DOCTYPE html>

<%# Flash errors if %>
<% if @article.errors.any? %>
<h3>The Following Errors Provented Your submission</h3>
<ul>
    <% @article.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
</ul> 
<% end %>
<%# Submittion of create/add %>

<%# Form css addition %>
<div class='row'>
    <div class ='col-xs-12'>
        <%= form_for(@article, :html => {class: "form-horizontal", role: "form"}) do |i| %>
        <div class ="form-group">
            <div class ="control-label col-sm-2"
            <%= i.label :title %>
        </div>
        <div class ="col-sm-5">
            <%= i.text_field :title, class: "form-control", placeholder: "Title of article", autofocus: true %> 
        </div>
      </div>
      <div class ="form-group">
            <div class ="control-label col-sm-2"
            <%= i.label :description %>
            <br>
            </div>
            <br>
                <div class ="col-sm-7">
                    <%= i.text_area :description, rows: 10, class: "form-control", placeholder: "Body of article" %>
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                <br>
                <%= i.submit class: "btn btn-primary btn-lg" %>
                </div>
            </div>
        <% end %>
    
        <p align="center">[<%= link_to "Go Back To Articles", articles_path %>]
    </div>
</div>
</html>