<!DOCTYPE html>
<h1 align="center">Showing: <%= @article.title %></h1>
<div class="well col-xs-8 col-xs-offset-2">
<%# Info about creator %>
<ul class="listing">
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                <li>Created By: </li>
                <li><%= link_to gravatar_for(@article.user), "/users/#{@article.user[:id]}/show" %></li>
                <li class="article-title">
                    <%= link_to @article.user.username, "/users/#{@article.user[:id]}/show" %>
                </li>
                <li><small><%= pluralize(@article.user.articles.count, "article") if @article.user.articles %></small></li>
            </div>
        </div>
    </ul> 
<%# Info about creator %>

    <h4 class="center"><strong>Description:</strong></h4>
    <hr>
    <%= @article.description %>

<p>Description: <%= @article.description %></p>

    <%= link_to "Delete", article_path(@article), method: :delete, data: { confirm: "Are you sure you want to delete this article?"}, class: "btn btn-xs btn-danger" %> |
    <%= link_to 'Home', root_path, class: "btn btn-xs btn-primary" %> |
    <%= link_to 'Edit', edit_article_path, class: "btn btn-xs btn-success" %>
</div>
</html>