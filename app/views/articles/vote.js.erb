<% if current_user.liked? @articles %>
    $('.like-btn').addClass('liked);
<% else %>
    $('.like-btn').removeClass('liked');
<% end %>
    $('.likes-count').html("<%= @articles.get_upvotes.size %>")

