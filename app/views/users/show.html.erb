<h1 align="center"> Welcome To <%= @user.username %>'s page</h1>
<div class="row">
    <div class="col-md-4 col-md-offset-4 center">
    <%= gravatar_for @user, size: 150  %>
    </div>
</div>
<h4 align="center"><%= @user.username %>'s Articles</h4>
<div class="center">
    <%= will_paginate @user_articles %>
</div>
<p align="center"><%= link_to 'Go To Users List', users_index_path, class: "btn btn-xs btn-success" %> | <%= link_to 'Go To Articles List', articles_path, class: "btn btn-xs btn-primary" %></p>
<%# changing render 'articles/article', obj: @user.articles to below: %>
<%= render 'articles/article', obj: @user_articles %> 
<div class="center">
    <%= will_paginate @user_articles %>
</div>
